name: veteranapp
description: "VeteranApp"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.7 <4.0.0"

dependencies:
  flutter_compat:
    path: ./flutter_compat
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test_compat:
    path: ./flutter_test_compat
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
